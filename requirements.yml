name: tst_online
channels:
    - conda-forge
dependencies:
    # Base
    - pip
    - python>=3.7
    - numpy
    # Dataframes
    - pandas
    - geopandas
    # Plotting
    - bqplot==0.11.5
    # Interactive
    - nodejs
    - jupyter
    - jupyterlab
    - notebook
    - ipython
    - ipyleaflet<=0.9.2
    - ipywidgets
    # GEE
    - oauth2client
    - pycrypto
    - pip:
        - appmode
        - google-api-python-client
        - earthengine-api
        - lcmap-pyccd
